# Tomcat
server:
  tomcat:
    uri-encoding: UTF-8
    max-threads: 1000
    min-spare-threads: 30
  port: 9020
  connection-timeout: 500000

spring:
  # 环境 dev|test|prod
  profiles:
    active: prod
  application:
    name: jms